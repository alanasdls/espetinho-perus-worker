{
  "name": "espetinho-perus-worker",
  "version": "23.0.0",
  "private": true,
  "description": "Cloudflare Worker com fidelidade Supabase e pontos calculados sem frete",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "node --check worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
